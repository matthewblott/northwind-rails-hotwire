<h1>New Region</h1>

<controls>
  <%= button_tag "Create", form: "region_form", formaction: create_region_path(@region) %>
  <%= link_to "Back", index_region_path, role: "button" %>
</controls>

<%= render "form", disabled: false %>
