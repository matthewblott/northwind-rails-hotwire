<div style="height: 10px">
</div>

<%= form_with(url: false, :html => {:id => 'region_form'}) do %>
  <fieldset <%= 'disabled' if disabled %>>
    <%= text_field(@region, :id) %>
    <%= text_field(@region, :name) %>
  </fieldset>
<% end %>

