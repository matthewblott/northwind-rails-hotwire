<h1>Region</h1>
<%= turbo_stream_from @region %>

<controls>
  <%= link_to "Edit", edit_region_path(@region), role: "button" %>
  <%= link_to "Back", index_region_path, role: "button" %>
</controls>

<%= render "form", disabled: true %>
