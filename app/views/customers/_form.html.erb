<div style="height: 10px">
</div>

<%= form_with(url: false, :html => {:id => 'customer_form'}) do %>
  <fieldset <%= 'disabled' if disabled %>>
    <%= field(@customer, :id) %>
    <%= field(@customer, :company_name) %>
    <%= field(@customer, :contact_name) %>
    <%= field(@customer, :contact_title) %>
    <%= field(@customer, :phone) %>
    <%= field(@customer, :fax) %>    
  </fieldset>
<% end %>

