<%= form_with(model: address, url: false, :html => {:id => 'address_form'}) do |f| %>
  <fieldset>

    <%= field(address, :name) %> 
    <%= field(address, :address_line_1) %>
    <%= field(address, :address_line_2) %>
    <%= field(address, :postal_town) %> 
    <%= field(address, :county) %> 
    <%= field(address, :post_code) %> 
    <%= field(address, :country) %> 

  </fieldset>

<% end %>
