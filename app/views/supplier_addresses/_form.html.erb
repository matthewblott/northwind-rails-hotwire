<%= form_with(model: supplier_address) do |form| %>
  <% if supplier_address.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(supplier_address.errors.count, "error") %> prohibited this supplier_address from being saved:</h2>

      <ul>
        <% supplier_address.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
