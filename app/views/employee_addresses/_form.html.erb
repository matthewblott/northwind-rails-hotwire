<%= form_with(model: employee_address) do |form| %>
  <% if employee_address.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employee_address.errors.count, "error") %> prohibited this employee_address from being saved:</h2>

      <ul>
        <% employee_address.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
