<% content_for :title, "Edit Supplier" %>

<%= turbo_stream_from @supplier %>

<controls class="turbo-native-hidden">

  <%= button_tag "Edit", form: "supplier_form",
    formaction: update_supplier_path(params[:supplier_id]),
    data: { controller: 'bridge--edit', bridge__edit_target: "submit" },
    method: :patch %>

  <%= button_tag "Update", form: "supplier_form",
    formaction: update_supplier_path(params[:supplier_id]),
    data: { controller: 'bridge--save', bridge__save_target: "submit" },
    method: :patch %>

  <%#= button_tag "Delete", form: "delete_supplier_form",
    formaction: update_supplier_path(params[:supplier_id]),
    data: { controller: 'bridge--delete', bridge__delete_target: "foo" },
    method: :patch %>

  <%#= button_tag "Submit", form: "supplier_form",
    formaction: update_supplier_path(params[:supplier_id]),
    data: { controller: "bridge--form", bridge__form_target: "submit2" },
    method: :patch %>

  <%= link_to "Back", index_supplier_path(params[:customer_id]), role: "button" %>
  <!--
  <form
    method="post"
    data-controller="bridge--form"
    data-action="turbo:submit-start->bridge--form#submitStart turbo:submit-end->bridge--form#submitEnd">

    <label for="first-name" class="display --block space --bottom-s">What is your first name?</label>
    <input type="text" name="first-name" id="first-name" placeholder="Type your first name…" />

    <label for="last-name" class="display --block space --bottom-s">What is your last name?</label>
    <input type="text" name="last-name" id="last-name" placeholder="Type your last name…" />

    <button
      class="button"
      type="submit"
      data-bridge--form-target="submit"
      data-bridge-title="Submit">
      Submit Form
    </button>
  -->
</form>




</controls>

<%= form_with method: :delete,
  url: destroy_supplier_path(params[:supplier_id], params[:supplier_id]),
  :html => {:id => 'delete_supplier_form'} do |form| %>
  <input type="hidden" name="supplier_id" value=<%= params[:supplier_id] %>>
  <input type="hidden" name="customer_id" value=<%= params[params[:supplier_id]] %>>
<% end %>

<%= render "form", supplier: @supplier, disabled: false %>
